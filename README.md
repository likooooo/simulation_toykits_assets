- simulation_toykits_assets 专门用于存放 simulation_toykits 网页的资源文件, 解决 simulation_toykits 仓库因为二进制资源导致 git 仓库过大的问题。 

``` shell
git submodule add git@github.com:likooooo/simulation_toykits_assets.git assets
git submodule update --init --recursive
```
